{
  "name": "task-executor",
  "displayName": "Task Executor",
  "description": "Quickly discover and run npm/pnpm/yarn scripts from your package.json",
  "version": "0.0.1",
  "author": "David Vigo",
  "license": "MIT",
  "homepage": "https://github.com/dvigo/task-executor",
  "repository": {
    "type": "git",
    "url": "https://github.com/dvigo/task-executor"
  },
  "bugs": {
    "url": "https://github.com/dvigo/task-executor/issues"
  },
  "engines": {
    "vscode": "^1.108.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:task-executor.runScript"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "task-executor.runScript",
        "title": "Task Executor: Run Script"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.108.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.52.0",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
